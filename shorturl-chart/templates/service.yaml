apiVersion: v1
kind: Service
metadata:
  name: {{ include "shorturl-chart.fullname" . }}
  labels:
    {{- include "shorturl-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shorturl-chart.selectorLabels" . | nindent 4 }}
